# 🚫 이 워크플로우는 비활성화되었습니다
# Claude AI Code Review로 대체됨 (.github/workflows/claude-code-review.yml)

name: "[DISABLED] Gemini AI Code Review"

# 트리거 비활성화
on:
  workflow_dispatch:
    inputs:
      dummy:
        description: "This workflow is disabled"
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow has been replaced by Claude AI Code Review"
